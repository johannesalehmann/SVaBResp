mdp

label "obj" = s=0;

const int length=100;

module main
    s: [0..length] init 1;

    [] (s>0 & s<= length) -> (s'=s);
    [] (s>0 & s<= length) -> (s'=0);
    [] (s>0 & s < length) -> (s'=s + 1);
endmodule